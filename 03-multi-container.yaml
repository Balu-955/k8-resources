kind: Pod
apiVersion: v1
metadata:
  name: multi-container
spec:
  containers:
  - name: nginx
    image: nginx
  - name: alma
    image: "almalinux:9"
    command : ["sleep","2000"]

# for alma linux there is no mechanism directly to run the container for infinite time by default. which is why we are giving sleep command.

    